# ① 값을 받아 계산하는 함수를 정의
# ② 계산 결과를 print가 아니라 return으로 돌려주기
# ③ 호출 결과를 변수에 담기
# ④ 담은 값을 다음 계산·출력에 이어 쓰기


def calculator(value):
    return value


result = calculator(85.0)
print(result)

result = calculator(90.0)
print(result)
